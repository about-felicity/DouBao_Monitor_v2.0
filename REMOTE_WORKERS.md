# 分机单模型采集与回传

主机运行 `一键启动三模型监控.bat` 后，会自动监听 `8791` 并生成
`runtime/lan_result_pairing.json`。该文件含有局域网地址和回传令牌，不能提交到 Git。

主机只负责接收、分析和展示数据，不运行远端模型采集。主机面板地址为
`http://127.0.0.1:3000/`。

每台远端电脑只运行一个模型：`deepseek`、`yuanbao` 或 `wenxin`。蚂蚁阿福不在本次远端部署范围内。

## 推荐：生成免配置一键包

1. 主机先启动面板，确认 `8791` 接收器在线。
2. 主机首次部署时，以管理员身份运行 `配置面板局域网访问_管理员运行.bat`，放行专用网络的 TCP `8791` 和 UDP `8792`。
3. 主机双击 `生成三模型远端一键包.bat`。
4. 将 `remote_model_deploy_three_models` 中三个不同的 ZIP 分别复制到三台电脑，每台电脑只解压一个模型。
5. DeepSeek/元宝远端安装 Chrome、MuMu 和 Python；文心远端只需 Chrome 和 Python，不使用模拟器。然后双击包内的 `一键启动<模型>远端采集.bat`。
6. DeepSeek/元宝按面板提示完成账号检测；文心可直接启动，程序会搜索百度并抓取搜索结果中的 AI 回答正文与引用信源。主机地址和令牌已经写入，无需手工配置。

每个远端面板只允许启动打包时指定的模型；同一台电脑也有进程锁保护，不能同时运行第二个模型。
DeepSeek 每轮计划随机等待 92–118 秒；发送尝试前先持久化下一次允许发送时间，断电重启或发送结果不确定也不会提前重试。回答超时或网络故障时实际间隔可能更长。其他模型使用各自默认间隔。
三个采集器都只把质量校验通过的成功结果入库和回传；临时断线、网页同步失败或异常回答只写本地日志，并重试原题，不污染信源统计。

## 主机 IP 自动变化

远端包会先尝试包内地址和主机名。若主机通过 DHCP 获得了新 IP，旧地址全部失败后，远端会通过 UDP `8792` 在当前局域网自动发现主机。发现响应必须通过内置回传令牌的 HMAC 验签；验证成功后，远端会把新地址原子写回自己的 `runtime/remote_workers/<模型>_sync.json`，随后继续补传 `outbox` 中尚未确认的数据，不需要重新生成或复制部署包。

自动发现要求主机和远端电脑位于允许广播互通的同一局域网；若路由器启用了 AP 隔离、访客网络隔离或跨 VLAN，则广播会被网络设备阻断，需要关闭隔离或给主机设置 DHCP 地址保留。

已经复制过旧版远端包的电脑需要用本次生成的新 ZIP 覆盖一次；升级完成后，后续主机 IP 变化不再需要重新复制或配置。

## 开发模式

1. 在远端电脑拉取与主机相同版本的项目。DeepSeek/元宝启动该模型所需的 MuMu 实例和 Chrome；文心只启动 Chrome。
2. 将主机的 `runtime/lan_result_pairing.json` 复制到远端电脑。
3. 执行 `远端单模型采集.bat <模型ID> <配对文件路径>`。默认连续采集；按 `Ctrl+C` 安全停止。

例如：`远端单模型采集.bat yuanbao D:\lan_result_pairing.json`。如需限制轮数，在末尾加入第三个参数，例如 `10`。

远端会先持久化每条结果到 `runtime/remote_workers/<模型>/outbox`，再由后台线程回传。
网络中断不会影响采集；只有主机按 `request_id` 确认入库后，远端才会移除该条队列。
主机将数据追加到原有模型 JSONL，统一面板无需额外配置即可看到新结果。
